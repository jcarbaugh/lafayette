# Full-screen grey; any response opens the hopper. The composites have no
# duration and the experiment does not correct, so this is the only key it
# reads. Everything else it needs comes from application.properties.

reinforcement_duration = 2000
