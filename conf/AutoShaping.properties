# Autoshaping with blue and red keys. Only the inter-trial interval is
# configurable: the key presentation is a hardcoded 10 s in getNextComposite(),
# and getReinforcementDuration() is hardcoded to 4000 in the experiment class,
# so setting reinforcement_duration here would have no effect.
#
# A long variable ITI against a short key presentation is the usual
# autoshaping arrangement.

rest_min = 15000
rest_max = 45000
