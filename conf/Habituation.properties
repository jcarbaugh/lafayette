# The hopper is held open continuously and there are no discriminative
# stimuli, so this is the only key the experiment reads. Everything else it
# needs comes from application.properties.

reinforcement_duration = 2000
